#include "main.h"
#include <stdio.h>

/**
 * _strlen - function which returns length of a string
 * @s: s is a character
 * Return: i is value
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
